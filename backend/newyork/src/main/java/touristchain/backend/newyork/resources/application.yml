server:
  port: 9004   # Forséti en 9004

spring:
  mvc:
    problemdetails:
      enabled: true

  datasource:
    url: jdbc:mysql://localhost:3306/touristchaindb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: Tiuristchain        # 👈 pon aquí tu usuario de MySQL
    password: "Touristchain2024!Secure#"        # 👈 pon aquí tu clave de MySQL
    driver-class-name: com.mysql.cj.jdbc.Driver

forseti:
  api-prefix: /api/v1/forseti   # prefijo oficial para Forséti

cors:
  allowed-origins: http://localhost:8080
